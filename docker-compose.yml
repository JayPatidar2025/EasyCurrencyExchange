version: "3.8"
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - FIXER_API_KEY=${FIXER_API_KEY}
      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASS}@${DB_HOST}:5432/${DB_NAME}
      - SPREAD_PERCENT=1.5
    env_file:
      - .env

volumes:
  pgdata:
